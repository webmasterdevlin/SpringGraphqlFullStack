type Project {
    id: ID
    name: String
    description: String
    status: Status
    client: Client
}

enum Status {
    pending
    ongoing
    completed
}

input ProjectInput {
    name: String
    description: String
    status: Status
    clientId: ID
}