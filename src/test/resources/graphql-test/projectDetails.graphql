query projectDetails($id: ID!) {
    project(id: $id) {
        id
        name
        description
        status
        client {
            id
            name
            email
            phone
        }
    }
}
